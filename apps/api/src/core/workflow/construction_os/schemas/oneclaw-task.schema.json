{
  "$id": "constructionOsOneclawTaskV1",
  "type": "object",
  "additionalProperties": false,
  "required": ["reply", "shouldExecute", "oneclawTask"],
  "properties": {
    "reply": { "type": "string", "minLength": 1, "maxLength": 5000 },
    "shouldExecute": { "type": "boolean" },
    "oneclawTask": {
      "anyOf": [
        { "type": "null" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["taskName", "steps"],
          "properties": {
            "taskName": { "type": "string", "minLength": 1, "maxLength": 120 },
            "steps": {
              "type": "array",
              "minItems": 1,
              "maxItems": 30,
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["id", "action", "input"],
                "properties": {
                  "id": { "type": "string", "pattern": "^[a-zA-Z0-9_-]{2,80}$" },
                  "action": {
                    "type": "string",
                    "enum": [
                      "content.generate",
                      "content.transform",
                      "result.compose",
                      "api.request",
                      "api.webhook",
                      "browser.open",
                      "browser.screenshot",
                      "browser.extract",
                      "browser.click",
                      "browser.type",
                      "file.read",
                      "file.write",
                      "file.append",
                      "file.list",
                      "message.draft",
                      "message.notify",
                      "message.send",
                      "human.approval.request",
                      "human.confirmation.request",
                      "construction.task.create",
                      "construction.approval.request",
                      "construction.procurement.followup",
                      "construction.inspection.create",
                      "construction.hse.corrective_action",
                      "construction.qaqc.ncr.create",
                      "construction.rfi.create",
                      "construction.change_order.prepare",
                      "construction.schedule.recovery_plan",
                      "construction.contract.claim_prepare",
                      "construction.budget.variance_review"
                    ]
                  },
                  "input": { "type": "object", "additionalProperties": true },
                  "dependsOn": {
                    "type": "array",
                    "default": [],
                    "items": { "type": "string", "pattern": "^[a-zA-Z0-9_-]{2,80}$" },
                    "uniqueItems": true
                  }
                }
              }
            }
          }
        }
      ]
    }
  },
  "allOf": [
    {
      "if": { "properties": { "shouldExecute": { "const": true } }, "required": ["shouldExecute"] },
      "then": { "properties": { "oneclawTask": { "type": "object" } } },
      "else": { "properties": { "oneclawTask": { "type": "null" } } }
    }
  ]
}
